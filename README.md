# random_walk

Compile the program by using this compilation command:
```gcc main.c -o random_walk.out `pkg-config --libs --cflags sdl3` -lm```

You can run the program using the following format:
```./random_walk.out <NUM_AGENTS>```

Close the program by properly closing the program window.
